import { Form } from "./components/Form";

export const Contato = () => {
    return (
        <footer>
            <div></div>
            <div>
                <Form />
            </div>
        </footer>
    );
};
